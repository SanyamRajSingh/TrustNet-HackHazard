# TrustNet Backend Requirements
# ================================
# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database — SQLite (dev/render free-tier) + PostgreSQL (production)
sqlalchemy[asyncio]>=2.0.25
aiosqlite>=0.19.0
asyncpg>=0.29.0
alembic>=1.13.1

# Neo4j
neo4j>=5.15.0

# Redis & Caching (optional — app runs without it via sync fallback)
redis>=5.0.1

# Async HTTP
httpx>=0.26.0
aiohttp>=3.9.1

# Task Queue (optional — tasks run sync if Redis unavailable)
celery>=5.3.6

# Authentication & Security — PyJWT + bcrypt (no passlib, no python-jose)
PyJWT>=2.8.0
bcrypt>=4.1.2

# Data Validation
pydantic>=2.5.3
pydantic-settings>=2.1.0
email-validator>=2.1.0

# ML/AI
openai>=1.7.0

# Utilities
python-dotenv>=1.0.0
structlog>=23.2.0
tenacity>=8.2.3
dnspython>=2.4.2
tldextract>=5.1.1
phonenumbers>=8.13.27
python-dateutil>=2.8.2
requests>=2.31.0

# Testing
pytest>=7.4.4
pytest-asyncio>=0.21.1
pytest-mock>=3.12.0
httpx>=0.26.0

# Monitoring
sentry-sdk[fastapi]>=1.39.2
